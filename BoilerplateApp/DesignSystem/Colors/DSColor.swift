import SwiftUI

enum DSColor {
    static let background = Color(.systemBackground)
    static let accent = Color(red: 0.30, green: 0.39, blue: 0.94)
    static let card = Color(.secondarySystemBackground)
}
