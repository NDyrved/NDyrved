import SwiftUI

struct PrivacyPolicyView: View {
    var body: some View { ScrollView { Text("TODO: Replace with your privacy policy.").padding() }.navigationTitle("Privacy Policy") }
}

struct TermsOfServiceView: View {
    var body: some View { ScrollView { Text("TODO: Replace with your terms.").padding() }.navigationTitle("Terms of Service") }
}
